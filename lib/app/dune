; App: 結び目 (composition root)。
; Usecase が perform する Port の effect を、どの Gateway の関数で解釈するかを決め
; (composition_root.ml)、cohttp-eio のサーバーに配線する (server.ml)。全レイヤーを知っているのはここだけ。
; bin/ ではなくライブラリにしてあるのは、E2E テストから同じ配線を再利用するため。

(include_subdirs qualified)

(library
 (name user_api_app)
 (libraries
  user_api_driver
  user_api_gateway
  user_api_port
  user_api_rest
  cohttp-eio
  http
  eio
  logs))
