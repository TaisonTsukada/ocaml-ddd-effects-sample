; Gateway 層のテスト。
; 腐敗防止層の変換 (row <-> domain, Driver のエラー -> ドメインのエラー) と、
; 実 Driver を使った操作とその翻訳結果を見る。
; Port の action との束縛は composition root の責務なので test/app/ で見る。

(test
 (name test_user_gateway)
 (package user_api)
 (libraries
  user_api_domain
  user_api_driver
  user_api_gateway
  user_api_test_support
  alcotest))
